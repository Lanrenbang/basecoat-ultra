{
  "name": "basecoat-ultra-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:svelte": "bun run --filter @lanrenbang/basecoat-ultra-svelte dev",
    "dev:docs": "bun run --filter basecoat-ultra-docs dev",
    "cli": "bun packages/cli/src/index.js",
    "build:ultra": "bun run --filter @lanrenbang/basecoat-ultra build",
    "build:svelte": "bun run --filter @lanrenbang/basecoat-ultra-svelte build",
    "build:docs": "bun run --filter basecoat-ultra-docs build",
    "publish:ultra": "cp README.md LICENSE.md packages/ultra/ && bun run build:ultra && npm publish --access public -w packages/ultra",
    "publish:svelte": "cp README.md LICENSE.md packages/svelte/ && bun run build:svelte && npm publish --access public -w packages/svelte"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
